<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ds.bienestaruq">
    <!--Agregando permisos para que se pueda escoger una imagen del celular -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Principal"
            android:label="@string/title_activity_principal"
            android:theme="@style/AppTheme" />
        <activity android:name=".CrearEncargado" />
        <activity android:name=".CrearServicio" />
        <activity android:name=".CrearTipoServicio" />
        <activity
            android:name=".Usuario"
            android:label="@string/title_activity_usuario"
            android:theme="@style/AppTheme" />
        <activity android:name=".RegistroCliente" />
        <activity android:name=".RecuperarContrasena"></activity>
    </application>

</manifest>